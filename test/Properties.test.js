var Properties = require('../lib/Properties');
// var PropertiesService = require('./mocks/PropertiesService');
var assert = require('assert');

describe('Properties', function() {
  describe('loadProperties()', function() {
    xit('should get the properties document', function() {});
    xit('should assign properties to `this`', function() {});
    xit('should return human readable error if document is not parsable', function() {});
  });
  describe('saveProperties()', function() {
    xit('should stringify whatever props are sent to it', function() {});
    xit('should fail gracefully if properties cannot be serialized', function() {});
    xit('should update file with stringified props', function() {});
  });
});
